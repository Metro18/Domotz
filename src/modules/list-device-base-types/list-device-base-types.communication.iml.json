{
	// Request to API endpoint.
	"url": "/type/device/base",                       // Relative to base URL
	"method": "GET",
	"qs": {                                // Query string
	},

	// Response handling
	"response": {
		"limit": "{{parameters.limit}}",
		"iterate": "{{body}}",
		"output": "{{item}}"
	}
}