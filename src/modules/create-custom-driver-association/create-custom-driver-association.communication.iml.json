{
	// Request to API endpoint with parameter "id" defined in Mappable parameters.
	"url": "/custom-driver/{{parameters.custom_driver_id}}/agent/{{parameters.agent_id}}/device/{{parameters.device_id}}/association",      // Relative to base URL
	"method": "GET",
	"qs": {},                               // Query string

	// Response handling
	"response": {
		"output": "{{body}}"                // Return JSON response body as an output bundle.
	}
}