{
	// Request to API endpoint.
	"url": "/agent/{{parameters.agent_id}}/device", // Relative to base URL
	"method": "GET",
	"qs": {                                // Query string
		"show_hidden": "{{parameters.show_hidden}}"  // Parameter "show_hidden" that is defined in static parameters.
	},

	// Response handling
	"response": {
		"limit": "{{parameters.limit}}",
		"iterate": "{{body}}",
		"output": "{{item}}"
	}
}