{
	"url": "/agent/{{parameters.agent_id}}/device/{{parameters.device_id}}/variable/{{parameters.variable_id}}/history",
	"method": "GET",
	"qs": {
		"from": "{{parameters.from}}",
		"to": "{{parameters.to}}"
	},
	"response": {
		"output": "{{body}}"
	}
}
