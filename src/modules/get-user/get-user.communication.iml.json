{
	// Request to API endpoint with parameter "id" defined in Mappable parameters.
	"url": "/user",      // Relative to base URL
	"method": "GET",
	"qs": {},                               // Query string

	// Response handling
	"response": {
		"output": "{{body}}"                // Return JSON response body as an output bundle.
	}
}