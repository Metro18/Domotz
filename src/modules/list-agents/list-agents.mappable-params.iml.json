[
	{
		"name": "limit",
		"type": "uinteger",
		"label": "Maximum number of records",
		"help": "The maximum number of items to return. Default is 100.",
		"default": 100,
		"required": true
	},
	{
		"name": "display_name",        // Makes value accesible via "{{parameters.display_name}}".
		"type": "text",  // Sets the type to text.
		"label": "Display Name",  // Sets the user friendly label visible in the module.
		"help": "Consider only collectors with display_name containing the string (case insensitive).",
		"required": false
	},
	{
		"name": "team_name",        // Makes value accesible via "{{parameters.team_name}}".
		"type": "text",  // Sets the type to text.
		"label": "Team Name",  // Sets the user friendly label visible in the module.
		"help": "Filters by team name (companies only).",
		"required": false,
		"advanced": true
	}

]