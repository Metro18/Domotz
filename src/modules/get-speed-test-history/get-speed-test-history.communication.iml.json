{
	"url": "/agent/{{parameters.agent_id}}/history/network/speed",
	"method": "GET",
	"headers": {
		"X-Api-Key": "{{connection.apiKey}}"
	},
	"qs": {
		"from": "{{parameters.from}}",
		"to": "{{parameters.to}}"
	},
	"response": {
		"output": "{{body}}"
	}
}
