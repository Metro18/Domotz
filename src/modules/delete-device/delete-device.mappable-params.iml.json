// Defines "id" as mandatory module input parameter.
[
	{
		"name": "agent_id",        // Makes value accesible via "{{parameters.agent_id}}".
		"type": "uinteger",  // Set the type to unsigned integer.
		"label": "Collector ID",  // Sets the user friendly label visible in the module.
		"required": true     // Sets the parameter as mandatory.
	},
	{
		"name": "device_id",        // Makes value accesible via "{{parameters.device_id}}".
		"type": "uinteger",  // Set the type to unsigned integer.
		"label": "Device ID",  // Sets the user friendly label visible in the module.
		"required": true     // Sets the parameter as mandatory.
	}
]