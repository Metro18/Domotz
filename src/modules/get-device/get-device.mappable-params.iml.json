[
	// Defines "agent_id" as mandatory module input parameter.
	{
		"name": "agent_id",        // Makes value accesible via "{{parameters.id}}".
		"type": "uinteger",  // Sets the type to unsigned integer.
		"label": "Agent ID",  // Sets the user friendly label visible in the module.
		"required": true     // Sets the parameter as mandatory.
	},
	// Defines "device_id" as mandatory module input parameter.
	{
		"name": "device_id",        // Makes value accesible via "{{parameters.id}}".
		"type": "uinteger",  // Sets the type to unsigned integer.
		"label": "Device ID",  // Sets the user friendly label visible in the module.
		"required": true     // Sets the parameter as mandatory.
	}
]