{
	"url": "/agent/{{parameters.agent_id}}/device/{{parameters.device_id}}/variable",
	"method": "HEAD",
	"qs": {
		"value": "{{parameters.value}}",
		"path": "{{parameters.path}}",
		"has_history": "{{parameters.has_history}}",
		"metric": "{{parameters.metric}}"
	},
	"response": {
		"output": {
			"count": "{{headers['x-entities-count']}}"
		}
	}
}
